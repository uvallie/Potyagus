# v2 — Потягусь на відео

Усе, що потрібно, щоб продовжити відео-версію без повторного дослідження. Стан 2026-09-01.

## Що вже готово і перевірено

| Річ | Де | Стан |
|---|---|---|
| Нейтральні кадри (старт = кінець петлі) | `neutral/neutral-side.{svg,png}`, `neutral/neutral-3q.{svg,png}` | зелений `#7DB262` = трава лужка; PNG 1024² з `qlmanage` |
| Кліпи | `clips/idle.mp4`, `clips/reach.mp4`, `clips/chest.mp4` | Seedance 2.5, 720p, 6 с, петля замкнена (0.8 / 1.5 / 0.8 з 255) |
| Промпти, дослівно | `prompts/*.txt` | плюс `STYLE.txt` і `LOOP.txt` — блоки, з яких складається кожен промпт |
| Шот-лист на решту | `shots.json` | 15 вправ: ракурс, стартовий кадр, готовий промпт, параметри — можна подавати в `generate_video_batch` як є |
| Інструменти | `tools/loopcheck.sh`, `tools/sheet.sh` | перевірка петлі числом; контактний аркуш 12 кадрів |
| Інтеграція в застосунок | `web/overlay.html` | `<video>` у слоті гуся, м'яка маска, розмита галявина `#7DB262`, риг як запасний; прапорець `USE_CLIPS` |
| Порівняння моделей | `video-tests/` | MiniMax проти Seedance на тому ж кадрі й промпті |

Ідентифікатори Higgsfield (можуть протухнути — тоді перезалити PNG через `media_upload`):
- `neutral-side.png` → media `dc5f40ab-2a6e-4f6f-8203-e434008b6979`
- `neutral-3q.png` → media `10cc9f60-80e7-4483-8dfb-c6d981708fe4`
- Пресет, який сервіс підсуває замість генерації, — відхиляти: `declined_preset_id: 24bae836-2c4a-48e0-89b6-49fcc0b21612`

## Чого ще немає

- **Фронтального нейтрального кадру.** Для squat, jacks, knees, neck, neckroll, sidebend потрібен гусь
  строго анфас на `#7DB262`. Промпт — за зразком `prompts/neutral-3q.recraft.txt` зі словами
  «seen directly from the front, perfectly symmetrical». Recraft, 2.5 кредита, брати 3–4 варіанти:
  `background_color` спрацьовує не завжди — перевіряти заливку першого path.
- 15 кліпів зі `shots.json`. Seedance ≈ 26 кредитів кожен → ~390 плюс перегенерації.

## Як продовжити (покроково)

1. Згенерувати фронтальний нейтральний кадр, зберегти як `neutral/neutral-front.{svg,png}`.
2. `media_upload` → PUT → `media_confirm` для кадрів, яких ще немає в сховищі.
3. Пакетами по ≤12 подати кадри зі `shots.json` (`status: todo`) у `generate_video_batch`,
   підставивши `media_id` у `start_image` **і** `end_image`.
4. Скачати → `tools/loopcheck.sh` (має бути < 6) → `tools/sheet.sh` → переглянути аркуш:
   плоскі форми, без пір'я, рух читається. Що не так — перегенерувати, це нормальний відсоток.
5. Файли назвати за `pose` з `data/exercises.json` → `web/clips/<pose>.mp4`.
6. `USE_CLIPS = true` в `web/overlay.html`, `./bin/potyagus build`.
7. Перевірити в браузері на 960×620 і вживу через `potyagus now`; для idle-стану кліп `idle.mp4`.

## Що з'ясовано про моделі

- **Seedance 2.5** — обрана. Тримає пласкі форми, файли ~0.4 МБ за 6 с, ~4–5 хв рендер.
- **MiniMax H3 Max** — 12.5 кредита, амплітудніший, але домальовує окремі пера на розкритих
  крилах, файли втричі більші. Годиться для швидких проб руху.
- Жодна модель не покаже «руки за спиною» в пласкому фронтальному ракурсі — планувати
  такі рухи у три чверті або в профіль.
- Три чверті часто дрейфує в анфас за 6 секунд — якщо ракурс критичний, писати «keeps the
  same three-quarter angle throughout».

## Рішення, які вже прийняті (не переглядати без Аліни)

- Фон кліпів — зелений `#7DB262`, а не плам: збігається з травою, маска майже невидима.
- Без заходу/виходу з кадру: статична камера, замкнена петля — щогодини захід дратував би.
- Погляд у камеру — в `idle` (гусь чекає і дивиться на тебе); у вправах — хіба коротко в кінці.
- Ракурс кожної вправи — той, у якому рух видно (див. `shots.json`); півкола головою — тільки анфас.
